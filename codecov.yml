coverage:
  status:
    project:
      default:
        threshold: 1%    # allow up to 1% drop in overall coverage
    patch:
      default:
        target: 70%      # require 70% coverage on new/changed lines
